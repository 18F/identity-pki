---
workers/failed_jobs:
  logs:
    - ${env}_/srv/reporting/shared/log/workers.log
  query: |
    ## Shows jobs that raised an exception while running
    fields @timestamp, @message
    | filter ispresent(exception_class) and name = 'perform.active_job'
