#!/bin/bash

# Create an IAM user in the identity-master account!

# Strictish mode
set -euo pipefail

IAM_USERS_FILE="$(GIT_DIR)"

usage() {
  cat >&2 <<EOM
usage: $(basename "$0") USERNAME

Create an AWS login profile (temporary password + Access/Secret Keys) for USERNAME.

To work properly, USERNAME must:
  - be an entry in 

EOM
}
