#!/bin/bash

USE_RUN=1

# print $usage and return 1
badopt() {
  echo "Usage: ${usage}"
  break 1
}

# parse space/tab-separated column output and space with pipe marks
mdout() {
  echo "|$@|" | column -t | tr -s ' ' '|' | sed 's/ *| */@| /g'   | column -s '@' -t
}
