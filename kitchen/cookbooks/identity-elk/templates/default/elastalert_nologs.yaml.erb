name: No Logs
type: flatline
index: logstash-2*
alert_text: logs from logstash seem to have stopped entirely!
owner: <%= @env %>
threshold: 4000
realert:
    hours: 2
doc_type: _doc
timeframe:
    minutes: 20
use_count_query: true
filter:
- query:
    query_string:
      query: "*"
alert:
<% if ! @emails.empty? %>
- "email"
<% end %>
<% if ! @webhook.nil? %>
- "slack"
<% end %>
<% if ! @emails.empty? %>
email:
<%   @emails.each do |e| %>
- "<%= e %>"
<%   end %>
<% end %>
<% if ! @webhook.nil? %>
slack_webhook_url: <%= @webhook %>
slack_username_override: <%= "#{@env} alerts" %>
slack_channel_override: ["<%= @slackchannel %>"]
<% end %>
