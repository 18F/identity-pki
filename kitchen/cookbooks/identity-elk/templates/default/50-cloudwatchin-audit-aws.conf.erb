input {
  cloudwatch_logs {
    log_group => "/aws/lambda/audit-aws"
    region => "<%= @aws_region %>"
    type => "cw_<%= @log_group %>"
    sincedb_path => "/usr/share/logstash/.sincedb_cloudwatch_<%= @log_group %>"
    start_position => "end"
  }
}
