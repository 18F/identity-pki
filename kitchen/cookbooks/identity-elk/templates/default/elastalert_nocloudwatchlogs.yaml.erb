name: No CloudWatch Logs
type: flatline
index: logstash-cloudwatch*
alert_text: logs from cloudwatch in the <%= @env %> ELK instance seem to have stopped entirely!
owner: <%= @env %>
threshold: <%= @thresholds['cloudwatch'] %>
realert:
    hours: 2
doc_type: _doc
scan_entire_timeframe: true
timeframe:
    minutes: 20
use_count_query: true
alert:
<% if ! @emails.empty? %>
- "email"
<% end %>
<% if ! @webhook.nil? %>
- "slack"
<% end %>
<% if ! @emails.empty? %>
email:
<%   @emails.each do |e| %>
- "<%= e %>"
<%   end %>
<% end %>
<% if ! @webhook.nil? %>
slack_webhook_url: <%= @webhook %>
slack_username_override: <%= "#{@env} alerts" %>
slack_channel_override: ["<%= @slackchannel %>"]
<% end %>
