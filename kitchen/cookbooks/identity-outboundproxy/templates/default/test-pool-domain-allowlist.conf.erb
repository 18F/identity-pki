<%= render 'gitlab-domain-allowlist.conf.erb' %>

# Used by IDP "make lint_country_dialing_codes" to scrape AWS docs for Pinpoint Support by Country
docs.aws.amazon.com

# so code scanning works
cdn.registry.gitlab-static.net

# allow the public ECR
public.ecr.aws
